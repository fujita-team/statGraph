% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statGraph.R
\name{degree.based.graph.param.estimator}
\alias{degree.based.graph.param.estimator}
\title{Degree-based graph parameter estimator}
\usage{
degree.based.graph.param.estimator(
  Graph,
  model,
  lo = NULL,
  hi = NULL,
  eps = 0.001,
  from = NULL,
  to = NULL,
  npoints = 2000,
  numCores = 1
)
}
\arguments{
\item{Graph}{The undirected unweighted graph.}

\item{model}{Either a string or a function:

A string that indicates one of the following models: "ER" (Erdos-Renyi random
graph), "GRG" (geometric random graph), "WS" (Watts-Strogatz model), and "BA" (Barabasi-Albert model).

A function that returns a Graph generated by a graph model. It must contain two arguments: the first one
corresponds to the graph size and the second to the parameter of the model.}

\item{lo}{The smallest parameter value that the graph model can take.

If 'model' is a string, then the default value of '0' is used for the predefined models ("ER", "GRG", "WS", and "BA").}

\item{hi}{The largest parameter value that the graph model can take.

If 'model' is an string, then the default values are used for the predefined models: '1' for "ER", 'sqrt(2)' for "GRG", '1' for "WS", and '5' for "BA").}

\item{eps}{Desired precision of the parameter estimate.}

\item{from}{Lower end of the interval that contain the eigenvalues to generate the degree-based spectral densities. If this value is null, the smallest eigenvalue of the adjacency matrix corresponding to 'Graph' is then used.}

\item{to}{Upper end of the interval that contain the eigenvalues to generate the degree-based spectral densities. If this value is null, the largest eigenvalue of the adjacency matrix corresponding to 'Graph' is then used.}

\item{npoints}{The number of points to discretize the interval '\link{from, to}'.}

\item{numCores}{The number of cores to use for parallelization.}
}
\value{
Returns a list containing:
\item{param}{The degree-based parameter estimate. For the "ER", "GRG", "WS", and "BA"
models, the parameter corresponds to the probability to connect a pair of
vertices, the radius used to construct the geometric graph in a unit square,
the probability to reconnect a vertex, and the scaling exponent respectively.}
\item{dist}{The L1 distance between the observed graph and the graph model with the estimated value.}
}
\description{
'degree.based.graph.param.estimator' estimates the parameter that best approximates the model to the observed graph by using the degree-based spectral density and ternary search.
}
\examples{
require(igraph)
### Example giving only the name of the model to use
G <- igraph::sample_smallworld(dim = 1, size = 10, nei = 2, p = 0.2)
# Obtain the parameter of the WS model
estimated.parameter <- degree.based.graph.param.estimator(G,"WS",npoints = 10,numCores = 1)
estimated.parameter
### Example giving a function instead of a model
# Defining the model to use
K <- as.integer(igraph::ecount(G)/igraph::vcount(G))
fun_WS <- function(n, param, nei = K){
  return (igraph::sample_smallworld(dim = 1,size = n, nei = nei,p = param))
}
# Obtain the parameter of the WS model
estimated.parameter <- degree.based.graph.param.estimator(G, fun_WS, lo = 0, hi = 1, npoints = 10, numCores = 1)
estimated.parameter

}
\keyword{degree_based_parameter_estimation}
